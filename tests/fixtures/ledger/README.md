# Ledger

A small ledger helper. `total()` accepts positive and negative amounts and rounds the final sum to two decimal places. Amounts are ordinary numbers. Run `python -m unittest discover -s . -p 'test_*.py'`.
